{
  "name": "fixed-asset-registry",
  "version": "1.0.0",
  "description": "Fixed Asset Registry Application",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "start": "cd server && npm install && npm start",
    "postinstall": "cd server && npm install"
  }
}
